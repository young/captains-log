// Export some function that saves things to indexdb
// grab data from indexdb

function bestTypeOfPizza(
	type,
	secondType
) {
	console.log(`Best type of Pizza: ${type}`);
	console.log(`Second best type of Pizza: ${secondType}`);
}
